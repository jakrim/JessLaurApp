export default class Memory {
  constructor(id, title, location, description, image) {
    (this.id = id),
      (this.title = title),
      (this.location = location),
      (this.description = description),
      (this.image = image);
  }
}
